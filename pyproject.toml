[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dinov2-fire-hazard"
version = "1.0.0"
description = "Reproducible DINOv2 fire-hazard classification baseline"
requires-python = ">=3.10,<3.13"

[tool.setuptools.packages.find]
where = ["src"]
